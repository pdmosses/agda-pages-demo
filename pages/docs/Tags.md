# Tags

Any page can include the tags index by including the following HTML comment
with `...` replaced by `material/tags`:

```html
<!-- ... -->
```

Currently, only one page has metadata that includes a `tags` list:

<!-- material/tags -->
